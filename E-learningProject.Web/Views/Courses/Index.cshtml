@model IReadOnlyList<E_learningProject.Core.Entities.Module>

@{
    ViewData["Title"] = "Cours";
}

<div class="page-shell">
    <section class="page-hero">
        <div>
            <p class="eyebrow mb-2">Catalogue des cours</p>
            <h1 class="h2 mb-2">Des modules clairs, structurés et faciles à suivre.</h1>
            <p class="hero-subtitle">Parcourez les cours disponibles, consultez le détail d’un module et lancez la lecture en gardant un rythme simple.</p>
        </div>
        <div class="page-hero-actions">
            <a class="btn btn-brand" asp-controller="Quiz" asp-action="Index">Voir les quiz</a>
            <a class="btn btn-outline-dark" asp-controller="Discussion" asp-action="Index">Discussion</a>
        </div>
    </section>

    <div class="page-panel page-panel--soft d-flex justify-content-between align-items-center flex-wrap gap-3">
        <div>
            <p class="eyebrow mb-1">Accès public</p>
            <h2 class="h5 mb-1">Suivez un module à votre rythme</h2>
            <p class="mb-0 text-muted">Chaque carte résume le contenu et le nombre de leçons disponibles.</p>
        </div>
        <div class="d-flex gap-2 flex-wrap">
            <a class="btn btn-outline-dark" asp-controller="Quiz" asp-action="Index">Voir les quiz</a>
            <a class="btn btn-brand" asp-controller="Discussion" asp-action="Index">Lire les discussions</a>
        </div>
    </div>

    @if (!Model.Any())
    {
        <div class="page-panel text-center py-5">
            <div class="feature-icon mx-auto"><i class="bi bi-journal-x"></i></div>
            <h2 class="h4">Aucun module disponible</h2>
            <p class="text-muted mb-0">Les cours apparaîtront ici dès qu’un contenu sera publié.</p>
        </div>
    }
    else
    {
        <div class="content-grid content-grid--two">
            @foreach (var module in Model)
            {
                <article class="content-card h-100">
                    <div class="card-body d-flex flex-column">
                        <div class="d-flex justify-content-between align-items-start gap-3 mb-3">
                            <div>
                                <p class="eyebrow mb-2">Module</p>
                                <h2 class="h4 mb-1">@module.Title</h2>
                            </div>
                            <span class="status-chip status-chip--neutral">@module.Lessons.Count leçons</span>
                        </div>
                        <p class="text-muted flex-grow-1">@module.Description</p>
                        <div class="d-flex gap-2 flex-wrap mt-2">
                            <a class="btn btn-outline-dark" asp-controller="Courses" asp-action="Details" asp-route-id="@module.Id">Détails</a>
                            <a class="btn btn-brand" asp-controller="Courses" asp-action="Start" asp-route-moduleId="@module.Id">Commencer</a>
                        </div>
                    </div>
                </article>
            }
        </div>
    }
</div>