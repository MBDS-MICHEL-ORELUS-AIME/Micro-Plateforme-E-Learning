@using E_learningProject.Core.Enums
@model E_learningProject.Web.Models.TeacherWorkspaceViewModel

@{
    ViewData["Title"] = "Espace enseignant";
    var successMessage = TempData["TeacherSuccess"] as string;
    var hasModule = Model.TotalModules > 0;
    var hasLesson = Model.TotalLessons > 0;
    var hasMedia = Model.HasMediaSupport;
    var activeStep = !hasModule ? 1 : !hasLesson ? 2 : 3;
    var hasLatestModule = !string.IsNullOrWhiteSpace(Model.LatestModuleTitle);
    var hasLatestLesson = !string.IsNullOrWhiteSpace(Model.LatestLessonLabel);
    var progressPercent = !hasModule ? 0 : !hasLesson ? 33 : !hasMedia ? 66 : 100;
}

<div class="page-shell">
    <section class="page-hero">
        <div>
            <p class="eyebrow mb-2">Création pédagogique</p>
            <h1 class="h2 mb-2">Espace enseignant</h1>
            <p class="hero-subtitle">Créez vos quiz, ajoutez les médias de cours et gardez un aperçu de vos contenus récents.</p>
        </div>
        <div class="page-hero-actions">
            <a class="btn btn-brand" asp-controller="Teacher" asp-action="MyQuizzes">
                <i class="bi bi-list-check me-1"></i>Mes quiz
            </a>
            <a class="btn btn-outline-dark" asp-controller="Home" asp-action="Public">
                <i class="bi bi-house me-1"></i>Accueil
            </a>
        </div>
    </section>

    @if (!string.IsNullOrWhiteSpace(successMessage))
    {
        <div class="page-panel page-panel--soft status-chip status-chip--success">@successMessage</div>
    }

    <div class="page-panel page-panel--soft mb-4">
        <div class="section-heading">
            <div>
                <p class="eyebrow mb-1">Parcours guidé</p>
                <h2 class="h5 mb-1">Assistant de création en 3 étapes</h2>
                <p class="text-muted mb-0">Suivez le parcours: module, leçon, support. Vous pouvez revenir à tout moment.</p>
            </div>
        </div>

        <div class="mb-3">
            <div class="d-flex justify-content-between small text-muted mb-1">
                <span>Progression du parcours</span>
                <strong>@progressPercent%</strong>
            </div>
            <div class="progress" role="progressbar" aria-label="Progression parcours enseignant" aria-valuenow="@progressPercent" aria-valuemin="0" aria-valuemax="100" style="height:10px;">
                <div class="progress-bar bg-dark" style="width:@progressPercent%"></div>
            </div>
        </div>

        <div class="row g-2 mt-1 mb-2">
            <div class="col-12 col-md-6">
                <div class="status-chip status-chip--neutral w-100">
                    <strong>Dernier module:</strong> @(hasLatestModule ? Model.LatestModuleTitle : "Aucun module créé")
                </div>
            </div>
            <div class="col-12 col-md-6">
                <div class="status-chip status-chip--neutral w-100">
                    <strong>Dernière leçon:</strong> @(hasLatestLesson ? Model.LatestLessonLabel : "Aucune leçon créée")
                </div>
            </div>
        </div>

        <div class="row g-3 mt-1">
            <div class="col-12 col-md-4">
                <div class="metric-card h-100 @(activeStep == 1 ? "border border-2 border-dark" : string.Empty)">
                    <span>Étape 1</span>
                    <strong>Créer un module</strong>
                    <p class="text-muted small mb-0 mt-2">Donnez un titre et une description pour structurer votre cours.</p>
                    <div class="d-flex justify-content-between align-items-center mt-3">
                        <a class="btn btn-sm btn-outline-dark" href="#step-module">Aller à l'étape 1</a>
                        <span class="status-chip @(hasModule ? "status-chip--success" : "status-chip--warning")">
                            @(hasModule ? "OK" : "À faire")
                        </span>
                    </div>
                </div>
            </div>
            <div class="col-12 col-md-4">
                <div class="metric-card h-100 @(activeStep == 2 ? "border border-2 border-dark" : string.Empty)">
                    <span>Étape 2</span>
                    <strong>Ajouter une leçon</strong>
                    <p class="text-muted small mb-0 mt-2">Sélectionnez le module puis saisissez le contenu texte de la leçon.</p>
                    <div class="d-flex justify-content-between align-items-center mt-3">
                        @if (hasModule)
                        {
                            <a class="btn btn-sm btn-outline-dark" href="#step-lesson">Aller à l'étape 2</a>
                        }
                        else
                        {
                            <span class="btn btn-sm btn-outline-secondary disabled">Débloquez étape 1</span>
                        }
                        <span class="status-chip @(hasLesson ? "status-chip--success" : "status-chip--warning")">
                            @(hasLesson ? "OK" : hasModule ? "À faire" : "Verrouillée")
                        </span>
                    </div>
                </div>
            </div>
            <div class="col-12 col-md-4">
                <div class="metric-card h-100 @(activeStep == 3 ? "border border-2 border-dark" : string.Empty)">
                    <span>Étape 3</span>
                    <strong>Joindre un support</strong>
                    <p class="text-muted small mb-0 mt-2">Importez un PDF, une vidéo ou collez une URL externe.</p>
                    <div class="d-flex justify-content-between align-items-center mt-3">
                        @if (hasLesson)
                        {
                            <a class="btn btn-sm btn-outline-dark" href="#step-media">Aller à l'étape 3</a>
                        }
                        else
                        {
                            <span class="btn btn-sm btn-outline-secondary disabled">Débloquez étape 2</span>
                        }
                        @if (!hasLesson)
                        {
                            <span class="status-chip status-chip--warning">Verrouillée</span>
                        }
                        else if (hasMedia)
                        {
                            <span class="status-chip status-chip--success">OK</span>
                        }
                        else
                        {
                            <span class="status-chip status-chip--neutral">Finalisation</span>
                        }
                    </div>
                </div>
            </div>
        </div>
    </div>

    <div class="content-grid content-grid--two">
        <article class="metric-card">
            <span>Modules publies</span>
            <strong>@Model.TotalModules</strong>
        </article>
        <article class="metric-card">
            <span>Lecons disponibles</span>
            <strong>@Model.TotalLessons</strong>
        </article>
        <article class="metric-card">
            <span>Quiz actifs</span>
            <strong>@Model.TotalQuizzes</strong>
        </article>
        <article class="metric-card">
            <span>Discussions ouvertes</span>
            <strong>@Model.OpenDiscussions</strong>
        </article>
    </div>

    <div class="content-grid content-grid--two">
        <div class="page-panel" id="step-module">
            <div class="section-heading">
                <div>
                    <p class="eyebrow mb-1">Cours</p>
                    <h2 class="h5">Étape 1 - Créer un module</h2>
                    <p class="text-muted small mb-0">Ce module apparaîtra ensuite dans la liste de leçons et de quiz.</p>
                </div>
            </div>
            <form asp-controller="Teacher" asp-action="CreateModule" method="post" novalidate>
                @Html.AntiForgeryToken()
                <div class="row g-2">
                    <div class="col-12">
                        <label asp-for="ModuleForm.Title" class="form-label">Titre du module</label>
                        <input asp-for="ModuleForm.Title" class="form-control form-control-lg" />
                        <span asp-validation-for="ModuleForm.Title" class="text-danger small"></span>
                    </div>
                    <div class="col-12">
                        <label asp-for="ModuleForm.Description" class="form-label">Description</label>
                        <textarea asp-for="ModuleForm.Description" class="form-control" rows="4"></textarea>
                        <span asp-validation-for="ModuleForm.Description" class="text-danger small"></span>
                    </div>
                </div>
                <button type="submit" class="btn btn-brand mt-3">
                    <i class="bi bi-journal-plus me-1"></i>Créer le module
                </button>
                <a class="btn btn-link mt-3" href="#step-lesson">Passer à l'étape 2</a>
            </form>
        </div>

        <div class="page-panel" id="step-lesson">
            <div class="section-heading">
                <div>
                    <p class="eyebrow mb-1">Cours</p>
                    <h2 class="h5">Étape 2 - Ajouter une leçon</h2>
                    <p class="text-muted small mb-0">Le dernier module créé est déjà sélectionné automatiquement.</p>
                </div>
            </div>
            <form asp-controller="Teacher" asp-action="CreateLesson" method="post" novalidate>
                @Html.AntiForgeryToken()
                @if (!hasModule)
                {
                    <div class="alert alert-warning" role="alert">Créez d'abord un module pour débloquer cette étape.</div>
                }
                <fieldset disabled="@(!hasModule)">
                <div class="row g-2">
                    <div class="col-12">
                        <label asp-for="LessonForm.ModuleId" class="form-label">Module</label>
                        <select asp-for="LessonForm.ModuleId" class="form-select form-select-lg">
                            @foreach (var module in Model.ModuleOptions)
                            {
                                <option value="@module.Id">@module.Label</option>
                            }
                        </select>
                        <span asp-validation-for="LessonForm.ModuleId" class="text-danger small"></span>
                    </div>
                    <div class="col-12">
                        <label asp-for="LessonForm.Title" class="form-label">Titre de la leçon</label>
                        <input asp-for="LessonForm.Title" class="form-control form-control-lg" />
                        <span asp-validation-for="LessonForm.Title" class="text-danger small"></span>
                    </div>
                    <div class="col-12 col-md-4">
                        <label asp-for="LessonForm.Order" class="form-label">Ordre</label>
                        <input asp-for="LessonForm.Order" class="form-control" />
                        <span asp-validation-for="LessonForm.Order" class="text-danger small"></span>
                    </div>
                    <div class="col-12">
                        <label asp-for="LessonForm.TextContent" class="form-label">Contenu texte</label>
                        <textarea asp-for="LessonForm.TextContent" class="form-control" rows="5"></textarea>
                        <span asp-validation-for="LessonForm.TextContent" class="text-danger small"></span>
                    </div>
                </div>
                <button type="submit" class="btn btn-outline-dark mt-3">
                    <i class="bi bi-plus-square me-1"></i>Ajouter la leçon
                </button>
                </fieldset>
                <a class="btn btn-link mt-3" href="#step-media">Passer à l'étape 3</a>
            </form>
        </div>

        <div class="page-panel">
            <div class="section-heading">
                <div>
                    <p class="eyebrow mb-1">Quiz</p>
                    <h2 class="h5">Créer un quiz</h2>
                </div>
            </div>
            <form asp-controller="Teacher" asp-action="CreateQuiz" method="post" id="quiz-form" novalidate>
                @Html.AntiForgeryToken()
                <div asp-validation-summary="ModelOnly" class="text-danger mb-2"></div>

                <div class="row g-2 mb-3">
                    <div class="col-12 col-md-6">
                        <label asp-for="QuizForm.Title" class="form-label">Titre du quiz</label>
                        <input asp-for="QuizForm.Title" class="form-control form-control-lg" />
                        <span asp-validation-for="QuizForm.Title" class="text-danger small"></span>
                    </div>
                    <div class="col-6 col-md-3">
                        <label asp-for="QuizForm.PassingScore" class="form-label">Seuil de réussite (%)</label>
                        <input asp-for="QuizForm.PassingScore" class="form-control form-control-lg" />
                        <span asp-validation-for="QuizForm.PassingScore" class="text-danger small"></span>
                    </div>
                    <div class="col-6 col-md-3">
                        <label asp-for="QuizForm.ModuleId" class="form-label">Module</label>
                        <select asp-for="QuizForm.ModuleId" class="form-select form-select-lg">
                            @foreach (var module in Model.ModuleOptions)
                            {
                                <option value="@module.Id">@module.Label</option>
                            }
                        </select>
                        <span asp-validation-for="QuizForm.ModuleId" class="text-danger small"></span>
                    </div>
                </div>

                <div id="questions-container" class="d-grid gap-3"></div>

                <div class="d-flex gap-2 mt-3 flex-wrap">
                    <button type="button" class="btn btn-outline-dark" id="add-question-btn">
                        <i class="bi bi-plus-circle me-1"></i>Ajouter une question
                    </button>
                    <button type="submit" class="btn btn-brand">Enregistrer le quiz</button>
                </div>
            </form>
        </div>

        <div class="page-panel" id="step-media">
            <div class="section-heading">
                <div>
                    <p class="eyebrow mb-1">Médias</p>
                    <h2 class="h5">Étape 3 - Ajouter un support</h2>
                    <p class="text-muted small mb-0">Associez le PDF ou la vidéo à une leçon précise.</p>
                </div>
            </div>
            <form asp-controller="Teacher" asp-action="UploadMedia" method="post" enctype="multipart/form-data" novalidate>
                @Html.AntiForgeryToken()
                @if (!hasLesson)
                {
                    <div class="alert alert-warning" role="alert">Ajoutez d'abord une leçon pour débloquer l'ajout de support.</div>
                }
                <fieldset disabled="@(!hasLesson)">
                <div class="row g-2">
                    <div class="col-12">
                        <label asp-for="MediaForm.LessonId" class="form-label">Leçon cible</label>
                        <select asp-for="MediaForm.LessonId" class="form-select form-select-lg">
                            @foreach (var lesson in Model.LessonOptions)
                            {
                                <option value="@lesson.Id">@lesson.Label</option>
                            }
                        </select>
                        <span asp-validation-for="MediaForm.LessonId" class="text-danger small"></span>
                    </div>

                    <div class="col-12 col-md-6">
                        <label asp-for="MediaForm.PdfFile" class="form-label">Fichier PDF</label>
                        <input asp-for="MediaForm.PdfFile" class="form-control" type="file" accept="application/pdf" />
                    </div>

                    <div class="col-12 col-md-6">
                        <label asp-for="MediaForm.VideoFile" class="form-label">Fichier vidéo</label>
                        <input asp-for="MediaForm.VideoFile" class="form-control" type="file" accept="video/*" />
                    </div>

                    <div class="col-12">
                        <label asp-for="MediaForm.ExternalVideoUrl" class="form-label">Ou URL d’une vidéo externe</label>
                        <input asp-for="MediaForm.ExternalVideoUrl" class="form-control" placeholder="https://..." />
                    </div>
                </div>

                <button type="submit" class="btn btn-outline-dark mt-3">
                    <i class="bi bi-cloud-upload me-1"></i>Importer ou lier le média
                </button>
                </fieldset>
                <a class="btn btn-link mt-3" href="#step-module">Revenir à l'étape 1</a>
            </form>
        </div>

        <div class="page-panel">
            <div class="section-heading">
                <div>
                    <p class="eyebrow mb-1">Récent</p>
                    <h2 class="h5">Quiz récents</h2>
                </div>
            </div>
            @if (!Model.ExistingQuizzes.Any())
            {
                <p class="text-muted mb-0">Aucun quiz pour le moment.</p>
            }
            else
            {
                <ul class="list-group list-group-flush">
                    @foreach (var q in Model.ExistingQuizzes)
                    {
                        <li class="list-group-item d-flex justify-content-between align-items-center px-0">
                            <span>@q.Title</span>
                            <span class="status-chip status-chip--neutral">@q.QuestionCount question(s)</span>
                        </li>
                    }
                </ul>
            }
        </div>

        <div class="page-panel">
            <div class="section-heading">
                <div>
                    <p class="eyebrow mb-1">Communaute</p>
                    <h2 class="h5">Derniers fils de discussion</h2>
                </div>
            </div>
            @if (!Model.RecentDiscussionThreads.Any())
            {
                <p class="text-muted mb-0">Aucune discussion pour le moment.</p>
            }
            else
            {
                <ul class="list-group list-group-flush">
                    @foreach (var thread in Model.RecentDiscussionThreads)
                    {
                        <li class="list-group-item d-flex justify-content-between align-items-center px-0">
                            <a class="text-decoration-none" asp-controller="Discussion" asp-action="Details" asp-route-id="@thread.ThreadId">@thread.Title</a>
                            <span class="status-chip @(thread.IsResolved ? "status-chip--success" : "status-chip--warning")">
                                @thread.ReplyCount rep.
                            </span>
                        </li>
                    }
                </ul>
            }
        </div>
    </div>
</div>

<template id="question-template">
    <div class="card border question-item">
        <div class="card-body">
            <div class="d-flex justify-content-between align-items-center mb-2">
                <strong class="question-title"></strong>
                <button type="button" class="btn btn-sm btn-outline-danger remove-question-btn">Supprimer</button>
            </div>

            <div class="mb-2">
                <label class="form-label">Énoncé</label>
                <input class="form-control question-statement" type="text" />
            </div>

            <div class="mb-2">
                <label class="form-label">Type</label>
                <select class="form-select question-type">
                    <option value="MultipleChoice">Choix multiple</option>
                    <option value="TrueFalse">Vrai / Faux</option>
                    <option value="ShortAnswer">Réponse courte</option>
                </select>
            </div>

            <div class="options-container"></div>

            <button type="button" class="btn btn-sm btn-outline-primary add-option-btn mt-2">Ajouter une option</button>
        </div>
    </div>
</template>

<template id="option-template">
    <div class="row g-2 align-items-center option-item mt-1">
        <div class="col-8">
            <input type="text" class="form-control option-text" placeholder="Texte de l'option" />
        </div>
        <div class="col-3 form-check mt-0">
            <input class="form-check-input option-correct" type="checkbox" />
            <label class="form-check-label">Correcte</label>
        </div>
        <div class="col-1 text-end">
            <button type="button" class="btn btn-sm btn-link text-danger remove-option-btn">x</button>
        </div>
    </div>
</template>

@section Scripts {
    <partial name="_ValidationScriptsPartial" />
    <script>
        (function () {
            const container = document.getElementById('questions-container');
            const addQuestionBtn = document.getElementById('add-question-btn');
            const questionTemplate = document.getElementById('question-template');
            const optionTemplate = document.getElementById('option-template');

            function addOption(questionEl, optionData) {
                const optionsContainer = questionEl.querySelector('.options-container');
                const optionNode = optionTemplate.content.firstElementChild.cloneNode(true);
                optionNode.querySelector('.option-text').value = optionData?.text || '';
                optionNode.querySelector('.option-correct').checked = !!optionData?.isCorrect;

                optionNode.querySelector('.remove-option-btn').addEventListener('click', () => {
                    optionNode.remove();
                    syncNames();
                });

                optionsContainer.appendChild(optionNode);
            }

            function addQuestion(questionData) {
                const node = questionTemplate.content.firstElementChild.cloneNode(true);
                const statementInput = node.querySelector('.question-statement');
                const typeSelect = node.querySelector('.question-type');

                statementInput.value = questionData?.statement || '';
                typeSelect.value = questionData?.type || 'MultipleChoice';

                node.querySelector('.remove-question-btn').addEventListener('click', () => {
                    node.remove();
                    syncNames();
                });

                node.querySelector('.add-option-btn').addEventListener('click', () => {
                    addOption(node, null);
                    syncNames();
                });

                const options = questionData?.options?.length ? questionData.options : [{ text: '', isCorrect: false }, { text: '', isCorrect: false }];
                options.forEach(o => addOption(node, o));

                container.appendChild(node);
                syncNames();
            }

            function syncNames() {
                const questions = container.querySelectorAll('.question-item');
                questions.forEach((q, qi) => {
                    q.querySelector('.question-title').textContent = `Question ${qi + 1}`;
                    q.querySelector('.question-statement').name = `QuizForm.Questions[${qi}].Statement`;
                    q.querySelector('.question-type').name = `QuizForm.Questions[${qi}].Type`;

                    const options = q.querySelectorAll('.option-item');
                    options.forEach((o, oi) => {
                        o.querySelector('.option-text').name = `QuizForm.Questions[${qi}].Options[${oi}].Text`;
                        o.querySelector('.option-correct').name = `QuizForm.Questions[${qi}].Options[${oi}].IsCorrect`;
                    });
                });
            }

            addQuestionBtn.addEventListener('click', () => addQuestion(null));

            const initialQuestions = @Html.Raw(System.Text.Json.JsonSerializer.Serialize(Model.QuizForm.Questions.Select(q => new
            {
                statement = q.Statement,
                type = q.Type.ToString(),
                options = q.Options.Select(o => new { text = o.Text, isCorrect = o.IsCorrect })
            })));

            if (initialQuestions.length === 0) {
                addQuestion(null);
            } else {
                initialQuestions.forEach(q => addQuestion(q));
            }
        })();
    </script>
}
