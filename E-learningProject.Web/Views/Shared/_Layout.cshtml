<!DOCTYPE html>
<html lang="fr">
<head>
    <meta charset="utf-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>@ViewData["Title"] - E_learningProject.Web</title>
    <link rel="stylesheet" href="~/lib/bootstrap/dist/css/bootstrap.min.css" />
    <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap-icons@1.11.3/font/bootstrap-icons.min.css" />
    <link rel="stylesheet" href="~/css/site.css" asp-append-version="true" />
</head>
<body>
    @using Microsoft.AspNetCore.Http
    @{ 
        var currentController = Context.GetRouteData().Values["controller"]?.ToString();
        var currentAction = Context.GetRouteData().Values["action"]?.ToString();
        var currentUserName = Context.Session.GetString("CurrentUserName");
        var currentUserRole = Context.Session.GetString("CurrentUserRole");
        var isAuthenticated = !string.IsNullOrWhiteSpace(currentUserName);
        var isSuperAdmin = string.Equals(currentUserRole, "superadmin", StringComparison.OrdinalIgnoreCase);
        Func<string, string, bool> isActive = (controller, action) => string.Equals(currentController, controller, StringComparison.OrdinalIgnoreCase) && string.Equals(currentAction, action, StringComparison.OrdinalIgnoreCase);
    }
    <header>
        <nav class="navbar navbar-expand-sm navbar-toggleable-sm navbar-light bg-white border-bottom box-shadow mb-3">
            <div class="container-fluid">
                <a class="navbar-brand" asp-area="" asp-controller="Home" asp-action="Public">E-learning Project</a>
                <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target=".navbar-collapse" aria-controls="navbarSupportedContent"
                        aria-expanded="false" aria-label="Basculer la navigation">
                    <span class="navbar-toggler-icon"></span>
                </button>
                <div class="navbar-collapse collapse d-sm-inline-flex justify-content-between">
                    <ul class="navbar-nav flex-grow-1">
                        <li class="nav-item">
                            <a class="nav-link nav-pill @(isActive("Home", "Public") ? "active" : string.Empty)" asp-area="" asp-controller="Home" asp-action="Public"><i class="bi bi-house me-1"></i>Accueil</a>
                        </li>
                        <li class="nav-item">
                            <a class="nav-link nav-pill @(isActive("Courses", "Index") ? "active" : string.Empty)" asp-area="" asp-controller="Courses" asp-action="Index"><i class="bi bi-journal-bookmark me-1"></i>Cours</a>
                        </li>
                        <li class="nav-item">
                            <a class="nav-link nav-pill @(isActive("Quiz", "Index") ? "active" : string.Empty)" asp-area="" asp-controller="Quiz" asp-action="Index"><i class="bi bi-patch-question me-1"></i>Quiz</a>
                        </li>
                        <li class="nav-item">
                            <a class="nav-link nav-pill @(isActive("Discussion", "Index") ? "active" : string.Empty)" asp-area="" asp-controller="Discussion" asp-action="Index"><i class="bi bi-chat-dots me-1"></i>Discussion</a>
                        </li>
                        @if (isSuperAdmin)
                        {
                            <li class="nav-item">
                                <a class="nav-link nav-pill @(isActive("Admin", "Users") || isActive("Admin", "CreateUser") || isActive("Admin", "EditUser") ? "active" : string.Empty)" asp-controller="Admin" asp-action="Users"><i class="bi bi-people me-1"></i>Utilisateurs</a>
                            </li>
                        }
                    </ul>
                    <ul class="navbar-nav">
                        @if (isAuthenticated)
                        {
                            <li class="nav-item d-flex align-items-center px-2 text-muted small">
                                <i class="bi bi-person-circle me-1"></i>@currentUserName
                            </li>
                            <li class="nav-item">
                                <form asp-controller="Account" asp-action="Logout" method="post" class="d-inline">
                                    @Html.AntiForgeryToken()
                                    <button type="submit" class="nav-link nav-pill btn btn-link border-0 p-0 px-3">
                                        <i class="bi bi-box-arrow-right me-1"></i>Se deconnecter
                                    </button>
                                </form>
                            </li>
                        }
                        else
                        {
                            <li class="nav-item">
                                <a class="nav-link nav-pill @(isActive("Account", "Login") ? "active" : string.Empty)" asp-controller="Account" asp-action="Login"><i class="bi bi-box-arrow-in-right me-1"></i>Connexion</a>
                            </li>
                            <li class="nav-item">
                                <a class="nav-link nav-pill @(isActive("Account", "Register") ? "active" : string.Empty)" asp-controller="Account" asp-action="Register"><i class="bi bi-person-plus me-1"></i>Inscription</a>
                            </li>
                        }
                    </ul>
                </div>
            </div>
        </nav>
    </header>
    <div class="container">
        <main role="main" class="pb-3">
            @RenderBody()
        </main>
    </div>

    <footer class="border-top footer text-muted">
        <div class="container">
            &copy; 2026 - E-learning Project
        </div>
    </footer>
    <script src="~/lib/jquery/dist/jquery.min.js"></script>
    <script src="~/lib/bootstrap/dist/js/bootstrap.bundle.min.js"></script>
    <script src="https://unpkg.com/htmx.org@1.9.12"></script>
    <script src="~/js/site.js" asp-append-version="true"></script>
    @await RenderSectionAsync("Scripts", required: false)
</body>
</html>
