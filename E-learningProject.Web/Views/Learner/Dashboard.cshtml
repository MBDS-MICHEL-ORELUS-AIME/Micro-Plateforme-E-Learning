@model E_learningProject.Web.Models.LearnerDashboardViewModel

@{
    ViewData["Title"] = "Tableau de bord apprenant";
}

<div class="page-shell">
    <section class="page-hero">
        <div>
            <p class="eyebrow mb-2">Suivi personnel</p>
            <h1 class="h2 mb-2">Tableau de bord apprenant</h1>
            <p class="hero-subtitle">Bonjour, @Model.StudentId. Retrouvez votre progression et reprenez vos modules en un clic.</p>
        </div>
        <div class="d-flex gap-2 flex-wrap">
            <a class="btn btn-brand" asp-controller="Quiz" asp-action="Index">
                <i class="bi bi-patch-question me-1"></i>Aller aux quiz
            </a>
            <a class="btn btn-outline-dark" asp-controller="Learner" asp-action="QuizHistory">
                <i class="bi bi-clock-history me-1"></i>Voir mon historique
            </a>
        </div>
    </section>

    <div class="page-panel">
        <div class="d-flex justify-content-between align-items-center mb-2">
            <strong>Progression globale</strong>
            <span>@Model.OverallProgress%</span>
        </div>
        <div class="progress" style="height: 12px;">
            <div class="progress-bar" style="width: @Model.OverallProgress%"></div>
        </div>
    </div>

    <div class="content-grid content-grid--two">
        <article class="metric-card">
            <span>Modules termines</span>
            <strong>@Model.CompletedModules / @Model.Modules.Count</strong>
        </article>
        <article class="metric-card">
            <span>Certificats obtenus</span>
            <strong>@Model.CertificatesEarned</strong>
        </article>
        <article class="metric-card">
            <span>Tentatives de quiz</span>
            <strong>@Model.QuizAttempts</strong>
        </article>
        <article class="metric-card">
            <span>Quiz reussis</span>
            <strong>@Model.PassedQuizzes</strong>
        </article>
    </div>

    <div class="content-grid content-grid--two">
        @foreach (var module in Model.Modules)
        {
            <article class="content-card h-100">
                <div class="card-body d-flex flex-column">
                    <div class="d-flex justify-content-between align-items-start mb-2">
                        <h2 class="h5 mb-0">@module.Title</h2>
                        <span class="status-chip status-chip--neutral">@module.ProgressPercent%</span>
                    </div>

                    <p class="text-muted small flex-grow-1">@module.Description</p>

                    <div class="small mb-2">@module.ReadLessons / @module.TotalLessons leçons terminées</div>
                    <div class="progress mb-3" style="height: 8px;">
                        <div class="progress-bar" style="width: @module.ProgressPercent%"></div>
                    </div>

                    <a class="btn btn-brand btn-sm" asp-controller="Learner" asp-action="Reader" asp-route-moduleId="@module.ModuleId">
                        <i class="bi bi-book me-1"></i>Ouvrir le lecteur
                    </a>
                </div>
            </article>
        }
    </div>
</div>
