@model E_learningProject.Web.Models.LearnerBadgesViewModel
@{
    ViewData["Title"] = "Mes badges";
    Layout = "~/Views/Shared/_Layout.cshtml";
}

<div class="container py-4">
    <div class="d-flex justify-content-between align-items-center mb-4">
        <h2 class="h4 fw-bold"><i class="bi bi-award-fill text-warning me-2"></i>Mes badges</h2>
        <a href="@Url.Action("Dashboard", "Learner")" class="btn btn-outline-secondary btn-sm">
            <i class="bi bi-arrow-left me-1"></i>Tableau de bord
        </a>
    </div>

    @if (!Model.Badges.Any())
    {
        <div class="text-center py-5">
            <i class="bi bi-trophy display-1 text-muted"></i>
            <p class="mt-3 text-muted">Aucun badge pour l'instant. Lisez des leçons pour en débloquer !</p>
        </div>
    }
    else
    {
        <div class="row g-4">
            @foreach (var badge in Model.Badges)
            {
                <div class="col-12 col-sm-6 col-md-4 col-lg-3">
                    <div class="card h-100 border-0 shadow-sm text-center p-3">
                        <div class="fs-1 text-warning mb-2">
                            <i class="bi @badge.IconCss"></i>
                        </div>
                        <h6 class="fw-semibold">@badge.Name</h6>
                        <p class="small text-muted mb-1">@badge.Description</p>
                        <small class="text-secondary">
                            <i class="bi bi-calendar3 me-1"></i>@badge.AwardedAt.ToLocalTime().ToString("dd/MM/yyyy")
                        </small>
                    </div>
                </div>
            }
        </div>
    }
</div>
